# Convenience wrapper — same as deploy/e2e-tester/docker-compose.yml
#   docker compose -f docker-compose.tester.yml --env-file deploy/e2e-tester/.env up -d --build

include:
  - path: deploy/e2e-tester/docker-compose.yml
