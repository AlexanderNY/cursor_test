# HTTP-only. Блок 443 добавить после выпуска TLS (см. deploy/ui-edge/certs/9to18/).

server {
    listen 80;
    server_name 9to18.ru www.9to18.ru;

    location / {
        proxy_pass http://ui_9to18;
        include /etc/nginx/includes/proxy-ui.conf;
    }
}
